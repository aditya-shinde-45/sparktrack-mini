name: Deploy Backend to AWS Lambda

on:
  push:
    branches: [ main, master ]
    paths: [ 'reviewpanel-backend/**' ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: |
        cd reviewpanel-backend
        npm ci --production --omit=dev
        
    - name: Create deployment package
      run: |
        cd reviewpanel-backend
        ls -la node_modules/@vendia/
        zip -r ../deployment.zip . -x "*.git*" "*.log" "test/**"
        
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1
        
    - name: Deploy to Lambda
      run: |
        aws lambda update-function-code \
          --function-name sparktrack \
          --zip-file fileb://deployment.zip
          
    - name: Update Lambda Environment Variables
      run: |
        aws lambda update-function-configuration \
          --function-name sparktrack \
          --environment 'Variables={"SUPABASE_URL":"https://gmtajoqjbetveyluklpa.supabase.co","SUPABASE_ANON_KEY":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdtdGFqb3FqYmV0dmV5bHVrbHBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTQ3Mzg5MTEsImV4cCI6MjA3MDMxNDkxMX0.DFqIihnH9kTS1LBHJ3gSRjYueHn8uSx1hdpOfKlx0uM","SUPABASE_SERVICE_ROLE_KEY":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdtdGFqb3FqYmV0dmV5bHVrbHBhIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1NDczODkxMSwiZXhwIjoyMDcwMzE0OTExfQ.GOkDckkgt7b9DLxfA6L2bAxwM7IpXbEvoDDmQJa67yw","JWT_SECRET":"supersecretkey123","NODE_ENV":"lambda"}'