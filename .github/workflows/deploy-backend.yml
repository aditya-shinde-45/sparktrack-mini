name: Deploy Backend to AWS Lambda

on:
  push:
    branches: [ main, master ]
    paths: [ 'reviewpanel-backend/**' ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: |
        cd reviewpanel-backend
        npm ci --production --omit=dev
        
    - name: Create deployment package
      run: |
        cd reviewpanel-backend
        ls -la node_modules/@vendia/
        zip -r ../deployment.zip . -x "*.git*" "*.log" "test/**"
        
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1
        
    - name: Deploy to Lambda
      run: |
        aws lambda update-function-code \
          --function-name sparktrack \
          --zip-file fileb://deployment.zip
          
    - name: Update Lambda Environment Variables
      run: |
        aws lambda update-function-configuration \
          --function-name sparktrack \
          --environment Variables='{"SUPABASE_URL":"${{ secrets.SUPABASE_URL }}","SUPABASE_ANON_KEY":"${{ secrets.SUPABASE_ANON_KEY }}","SUPABASE_SERVICE_ROLE_KEY":"${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}","JWT_SECRET":"${{ secrets.JWT_SECRET }}","NODE_ENV":"lambda","EMAIL_USER":"${{ secrets.EMAIL_USER }}","EMAIL_PASSWORD":"${{ secrets.EMAIL_PASSWORD }}"}'